version: '3.9'

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - dbdata:/app/dbdata
    environment:
      - SQLITE_DB_PATH=/app/dbdata/db.sqlite3
    depends_on:
      - web

volumes:
  dbdata:
